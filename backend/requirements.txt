# ==========================================================================
# CivicTwin Backend — Python Dependencies
# Deployed on Render (free tier). NO torch/ML libs here.
# ML inference is proxied to Hugging Face Spaces via httpx.
# ==========================================================================

# --- Web Framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0

# --- Database (Supabase PostGIS) ---
sqlalchemy>=2.0,<3.0
geoalchemy2>=0.15,<1.0
psycopg2-binary>=2.9,<3.0
asyncpg>=0.30,<1.0
alembic>=1.14,<2.0

# --- Data Validation & Settings ---
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0

# --- HTTP & Inference Proxy ---
httpx>=0.28,<1.0
gradio-client>=1.5,<2.0

# --- Caching ---
redis>=5.0,<6.0

# --- Geospatial Utilities ---
geojson>=3.1,<4.0
shapely>=2.0,<3.0

# --- Environment ---
python-dotenv>=1.0,<2.0
